//import axios from 'axios';
//import { getSsmValue } from '../utils/ssm';

export async function validateSubscriptionForPhoneNumberInclusion(
    subscriptionName: string,
): Promise<boolean> {
    subscriptionName;
    return true;
}
